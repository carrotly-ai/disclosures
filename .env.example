# SEC requires a descriptive User-Agent containing contact information.
# Used for all US SEC EDGAR calls; GLEIF needs no credential.
DISCLOSURES_USER_AGENT="Your Organization your-email@example.com"

# Optional per-jurisdiction API keys. Set only the ones you query; US/GLEIF
# calls work without any of these.

# UK Companies House — required for all jurisdiction: "GB" operations.
# https://developer.company-information.service.gov.uk/
COMPANIES_HOUSE_API_KEY=""

# South Korea DART / OpenDART — required for all jurisdiction: "KR" operations.
# https://opendart.fss.or.kr/
OPENDART_API_KEY=""

# Japan EDINET (API v2) — required for jurisdiction: "JP" document search.
# JP CompanyResolve works without it (the EDINET code list is public).
# https://api.edinet-fsa.go.jp/
EDINET_API_KEY=""

# China cninfo (jurisdiction: "CN") and India BSE (jurisdiction: "IN") need no
# API key. BSE's host is anti-bot protected; if the default fetch is throttled,
# inject a browser-backed fetchFn via AdapterOptions rather than setting a key.

# UK FCA National Storage Mechanism (DTR5/TR-1 major holdings, inside GB
# CompanyOwners) has no env var: it is inject-only. The FCA provides no public
# read API, so the default path never contacts data.fca.org.uk. To enable the
# TR-1 major-holdings section, inject your own NSM-capable fetchFn via
# AdapterOptions instead of configuring a credential here.
